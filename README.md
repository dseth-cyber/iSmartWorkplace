# iSmart Workplace 3.6.0

เพิ่มแชท LAN แบบผสม: Directory Server ดูแลรายชื่อและกลุ่ม ส่วนข้อความและไฟล์ส่งตรงแบบเข้ารหัสระหว่างเครื่อง ดู [คู่มือตั้งค่า LAN และข้อจำกัด](LAN-CHAT-GUIDE.md) ก่อนใช้งาน รุ่นนี้ยังคงกฎ Backup เดิมและข้อมูลในโฟลเดอร์ BackupKeeper เดิม
1. สรุปความเปลี่ยนแปลงในเวอร์ชัน v3.6.8แก้ปัญหาการเชื่อมต่อ Server ข้าม VLAN ไม่ได้:พบสาเหตุแท้จริงคือเครื่อง Client ที่เคย Enroll ผูกกับ Server เครื่องเดิมไว้ จะถูกบล็อกไม่ให้เชื่อมต่อกับ Server เครื่องใหม่โดยอัตโนมัติปรับให้แสดงหน้าต่างแจ้งเตือนทันทีเมื่อเชื่อมต่อไม่สำเร็จ เพื่อไม่ให้สับสนว่าระบบหา Server ไม่เจอเพิ่มปุ่ม "ล้างการลงทะเบียนเครื่องนี้" (Reset Enrollment):เพิ่มในเมนู เครือข่าย / เชื่อมต่อ Server ช่วยให้สลับไปเชื่อมต่อ Server ตัวใหม่ได้ทันทีจากหน้าจอโปรแกรม โดยไม่ต้องตามไปลบไฟล์คอนฟิกเองจดจำสถานะการตรวจ SHA-256:ติ๊กถูกที่ช่อง "ตรวจ SHA-256 กับผู้ดูแลแล้ว" ให้อัตโนมัติเมื่อเปิดหน้าต่างขึ้นมาใหม่ด้วย IP และ Fingerprint เดิมที่เคยยืนยันแล้ว2. ฟังก์ชันภาพรวมของระบบในเวอร์ชันนี้ระบบ Backup & File Management:รองรับงาน 4 รูปแบบ: ลบอย่างเดียว, Copy, Backup (แยกโฟลเดอร์ตามรอบเวลา พร้อมตัวเลือกบีบอัด Zip และนับจำนวนชุดเก็บ Retention), และ Sync ทางเดียวมีระบบทดลองสแกนจำลองผลลัพธ์ก่อนเริ่มทำงานจริงป้องกันไฟล์เสียหายด้วยการตรวจสอบความถูกต้องผ่าน SHA-256 Checksumระบบ LAN Chat (Hybrid P2P + Directory Server):ค้นหา Server ในวงเครือข่ายเดียวกันอัตโนมัติ (UDP Broadcast) หรือเชื่อมต่อแบบระบุ IP ข้าม VLANการส่งข้อความและไฟล์ส่งตรงระหว่างเครื่องแบบ P2P เข้ารหัส (รองรับไฟล์แนบสูงสุด 50 MB, แสดงตารางรูปภาพย่อ และตัวอย่างวิดีโอ)รองรับการคลิกขวา/ดับเบิลคลิกแยกหน้าต่างแชท (Detached Window)มีฟังก์ชันจับภาพหน้าจอ (Screenshot) ส่งลงแชทได้ทันทีตัว Server มี System Tray Icon สำหรับย่อเก็บหน้าต่าง Console3. การตั้งค่าเชื่อมต่อแบบ Manual ข้าม VLANช่องตั้งค่าค่ามาตรฐานที่ถูกต้องคำแนะนำการใช้งานIP ของ Server192.168.x.xใส่ IP จริงของเครื่อง Serverพอร์ต Server48731พอร์ตมาตรฐานของ Directory ServerSHA-256 ของ Serverรหัส 64 ตัวอักษรคัดลอกจากหน้าจอ Console ของ Server มากรอกIP เครื่องนี้0.0.0.0ผูกทุก Interface เครือข่ายในเครื่องพอร์ตรับข้อความ48733พอร์ตมาตรฐานสำหรับรับข้อความ P2Pตรวจ SHA-256ติ๊กถูกต้องติ๊กยืนยันก่อนกดบันทึก4. ขั้นตอนการติดตั้งและการใช้งานฝั่งเครื่องแม่ (Directory Server):แตกไฟล์ iSmartWorkplace-3.6.8-Windows-x64.zipดับเบิลคลิกเปิด iSmartWorkplace-Server.exe ทิ้งไว้ (คลิกขวาที่ Tray เพื่อสั่งซ่อนหน้าต่าง Console ได้)หากต้องการเปิดใช้งานฟังก์ชันแชทหรือสำรองข้อมูลบนเครื่อง Server ด้วย ให้ดับเบิลคลิกเปิด iSmartWorkplace.exe หรือติดตั้งผ่าน iSmartWorkplace-Setup.exeฝั่งเครื่องลูก (Client):วางไฟล์ iSmartWorkplace.exe ในตำแหน่งถาวร หรือติดตั้งผ่าน iSmartWorkplace-Setup.exe (ไม่ต้องติดตั้ง Python)หากอยู่ในวง LAN เดียวกัน โปรแกรมจะค้นหา Server อัตโนมัติหากอยู่คนละวง LAN / ข้าม VLAN ให้ไปที่เมนู เครือข่าย / เชื่อมต่อ Server กรอกข้อมูลตามตารางด้านบนแล้วกด "บันทึกและเชื่อมต่อ" (หากเคยเชื่อมต่อ Server ตัวอื่นมาก่อน ให้กด "ล้างการลงทะเบียนเครื่องนี้" ก่อน)การจัดการงาน Backup:งานเก่าจะปรากฏในเมนู โฟลเดอร์และตารางเวลา เป็นงานลบอย่างเดียวโดยอัตโนมัติ แนะนำให้สั่งพักตารางเวลาเพื่อตรวจสอบก่อนแนะนำให้กด "ทดลองสแกน" แล้วตรวจสอบรายการใน ประวัติการทำงาน → ดูรายละเอียด / CSV ก่อนเปิดทำงานจริงปิดหน้าต่างโปรแกรมเพื่อย่อลง System Tray ได้ตลอดเวลาสามารถตั้งค่าเริ่มโปรแกรมพร้อม Windows ได้ที่ ตั้งค่าและวิธีใช้ → เปิดโปรแกรมใน Tray เมื่อเข้าสู่ระบบ Windows
